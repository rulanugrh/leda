/*
Tenary operator ialah operator yang menyederhanakan sebuah if statement
*/
const num = 5
const res = num%2 == 0 ? 'Ini adalah Genap': 'Ini Bukan Genap';
console.log(res)

const Name = "ruls"
const result = Name == "kyo" ? 'My name is kiyo': 'No my name not kyo';
console.log(result)
